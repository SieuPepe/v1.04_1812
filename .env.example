# ========================================================================
# HydroFlow Manager v2.0 - Configuración de Base de Datos
# ========================================================================
#
# IMPORTANTE: Este archivo es una plantilla. Para usarlo:
#   1. Cópialo como .env en el directorio raíz del proyecto
#   2. Modifica los valores según tu instalación
#   3. NO incluyas el archivo .env en control de versiones
#
# ========================================================================

# ========================================================================
# CONFIGURACIÓN DEL SERVIDOR
# ========================================================================

# Host de la base de datos
# Ejemplos:
#   - localhost (servidor local)
#   - 127.0.0.1 (localhost por IP)
#   - 192.168.1.100 (servidor en red local)
#   - db.miempresa.com (servidor remoto por dominio)
DB_HOST=localhost

# Puerto de MySQL/MariaDB
# Por defecto MySQL usa 3306
# Algunas instalaciones usan 3307
# Ajusta según tu instalación
DB_PORT=3307

# ========================================================================
# CREDENCIALES (REQUERIDO)
# ========================================================================

# Usuario de base de datos
# IMPORTANTE: Debe tener permisos para:
#   - Crear esquemas y tablas
#   - SELECT, INSERT, UPDATE, DELETE en todos los esquemas
#   - Gestionar usuarios (para funciones administrativas)
DB_USER=root

# Contraseña de base de datos
# CAMBIA ESTE VALOR con tu contraseña real
# NUNCA subas este archivo con la contraseña real al repositorio
DB_PASSWORD=tu_contraseña_aqui

# ========================================================================
# ESQUEMAS DE BASE DE DATOS
# ========================================================================

# Esquema de gestión principal
# Contiene la tabla de proyectos y configuración global
DB_MANAGER_SCHEMA=manager

# Esquema de proyecto tipo/plantilla
# Se usa como plantilla para crear nuevos proyectos
DB_EXAMPLE_SCHEMA=proyecto_tipo

# Esquema por defecto para desarrollo y scripts
# Scripts de desarrollo usarán este esquema si no se especifica otro
DB_SCHEMA=cert_dev

# ========================================================================
# RENDIMIENTO
# ========================================================================

# Activar connection pooling para mejor rendimiento
# true:  Usa pool de conexiones (latencia ~1ms, recomendado para producción)
# false: Conexiones directas (latencia ~50ms, útil para debugging)
DB_USE_POOLING=true

# ========================================================================
# NOTAS IMPORTANTES
# ========================================================================
#
# 1. SEGURIDAD:
#    - El archivo .env NO debe incluirse en control de versiones
#    - Ya está configurado en .gitignore
#    - Cada desarrollador/servidor debe tener su propio .env
#
# 2. INSTALACIÓN LOCAL vs REMOTA:
#    - Local:  DB_HOST=localhost, DB_PORT=3306 o 3307
#    - Remota: DB_HOST=IP_o_dominio, DB_PORT=3306
#
# 3. PERMISOS NECESARIOS:
#    El usuario DB_USER debe tener permisos para:
#    - CREATE, DROP esquemas
#    - CREATE, ALTER, DROP tablas
#    - SELECT, INSERT, UPDATE, DELETE datos
#    - GRANT (para crear usuarios de proyecto)
#
# 4. PRODUCCIÓN:
#    Para servidores de producción, considera usar variables de
#    entorno del sistema en lugar de este archivo .env
#
# 5. VALORES POR DEFECTO:
#    Si una variable no está definida, se usarán estos defaults:
#    - DB_HOST: localhost
#    - DB_PORT: 3306
#    - DB_MANAGER_SCHEMA: manager
#    - DB_EXAMPLE_SCHEMA: proyecto_tipo
#    - DB_USE_POOLING: true
#
#    NOTA: DB_USER y DB_PASSWORD NO tienen default y son requeridos
#
# ========================================================================
