# Configuración de Base de Datos
# Copia este archivo a .env y configura tus valores

# Host de la base de datos (por defecto: localhost)
DB_HOST=localhost

# Puerto de MySQL (por defecto: 3307)
DB_PORT=3307

# Esquema de gestión principal (por defecto: manager)
DB_MANAGER_SCHEMA=manager

# Esquema de proyecto de ejemplo/plantilla (por defecto: proyecto_tipo)
DB_EXAMPLE_SCHEMA=proyecto_tipo

# Activar connection pooling para mejor rendimiento (por defecto: true)
# true: Usa pool de conexiones (latencia ~1ms, recomendado)
# false: Conexiones directas (latencia ~50ms)
DB_USE_POOLING=true

# IMPORTANTE: Este archivo .env NO debe incluirse en el control de versiones
# Añade .env al archivo .gitignore
