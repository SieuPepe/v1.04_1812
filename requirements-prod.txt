# HydroFlow Manager v2.0 - Production Requirements
# Dependencias mínimas para ejecutar la aplicación en producción
# Instalar con: pip install -r requirements-prod.txt

# Python >= 3.8 requerido

# Base de datos MySQL
mysql-connector-python>=8.0.0

# GUI Framework
customtkinter>=5.0.0
CTkMessagebox>=2.0.0
tkcalendar>=1.6.0

# Image processing
pillow>=10.0.0

# Plotting and visualization
matplotlib>=3.5.0

# Data processing
pandas>=1.3.0

# Export formats
openpyxl>=3.0.0
xlsxwriter>=3.0.0
python-docx>=0.8.0
reportlab>=3.6.0

# Word to PDF conversion (Windows only)
pywin32>=305; sys_platform == 'win32'

# Variables de entorno
python-dotenv>=1.0.0
